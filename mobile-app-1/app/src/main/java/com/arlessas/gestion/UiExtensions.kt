package com.arlessas.gestion

/**
 * Archivo intencionalmente conservado como punto de extensión futuro.
 *
 * Las funciones visuales compartidas quedaron centralizadas en UiEstilos.kt
 * y el helper dp() quedó en SistemaGestion.kt. Antes este archivo repetía
 * funciones como dp(), primaryButton(), field(), spinner(), baseScreen(),
 * setAnimatedClick(), etc.; esa duplicidad generaba errores de ambigüedad
 * en Kotlin al compilar los módulos separados.
 */
